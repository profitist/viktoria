# FEAT-0001-ISSUE-005: Решено

## Что исправлено
Токен при добавлении в URL WebSocket-соединения теперь оборачивается в `encodeURIComponent()`. Это предотвращает поломку URL при наличии в токене специальных символов (`.`, `+`, `/`, `=`).

## Файл
`frontend/lib/ws.ts`
